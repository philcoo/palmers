<%= form_for(palmer) do |f| %>
  <% if palmer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(palmer.errors.count, "error") %> prohibited this palmer from being saved:</h2>

      <ul>
      <% palmer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
